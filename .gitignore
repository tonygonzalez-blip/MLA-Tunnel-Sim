# Unreal Engine generated folders
# NOTE: Binaries/, Intermediate/, and Saved/ are excluded because they exceed
# GitHub's 100MB-per-file limit and are multi-GB. Build/ and DerivedDataCache/
# are tracked below.
Binaries/
Intermediate/
Saved/

# Compiled / build artifacts
*.tmp
*.pdb
*.obj

# OS / editor
.DS_Store
Thumbs.db
.vs/
.vscode/

# Keep source assets tracked (Config, Content, Source, .uproject)
